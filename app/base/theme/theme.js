import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      // main: '#70f',
      main: '#ae00ff',
    },
    secondary: {
      main: '#999',
    },
  },
})

export default theme
